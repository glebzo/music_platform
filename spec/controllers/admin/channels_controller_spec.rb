require 'rails_helper'

RSpec.describe Admin::ChannelsController, type: :controller do
  # it { should use_before_action :authenticate_listener! }
  # it { should use_before_action :check_is_admin? }

  describe '#index' do
    it 'renders all channels' do

    end
  end

end
